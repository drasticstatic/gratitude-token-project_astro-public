# gratitude-token-project_astro (private)

Seed commit so Intent can resolve a base branch and create a workspace against this repo.

Full scaffold — Astro site structure, `.github/dependabot.yml`, branch-protection ruleset,
and a `sync-public-allowlist.yml` workflow targeting `gratitude-token-project_astro-public` —
is queued for the Astro coordinator workspace. See `intent/workspaces/__chief__/PENDING-TASKS.md`
for the full task list and current status.
